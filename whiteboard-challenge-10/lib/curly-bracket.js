'use strict';

const stack = require('stack')

const solution = module.exports = {}

let string = ''
let curlyBracket = function (string) {
  let test = newStack
  for(let i=0; i < string.length; i++)
  if(!string) return null
  if(string)
  if(node === '{') 

}