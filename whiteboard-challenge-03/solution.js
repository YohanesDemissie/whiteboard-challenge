'use strict';

// function const traverse = (engine) => {
//     console.log()
   train = { 
        "value": 2, 
        "next": {
            "value": 16, 
            "next": null
    }
}

const traverse = (train) => {
    for(value=0; value === null; value++)
        return value.filter(NaN);
}