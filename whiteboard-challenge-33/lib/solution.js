let solution = module.exports = {};

solution.function = function (x, y)  {
let exponential = x^y //will finaliize this tomorrow
let firstValue = exponential.split
//apply array method to accumalte single digits into one new value
return //new value
}
